// Нека са дадени положителните естествени числа a, b и c. Да се напише булев израз, който има 
// стойност истина, ако квадратното уравнение ax^2 + bx + c = 0 има два реални, различни корена.
// Вход: 1 5 4,  Изход: 1
// Вход: 1 2 1,  Изход: 0

#include <iostream>

using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;

    cout << ((b * b - 4 * a * c) > 0) << endl;

    return 0;
}